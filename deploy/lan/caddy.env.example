# Caddy and the app receive the same generated proxy secret through separate,
# service-readable environment files outside the repository and workspace.
HOUSEBOOK_DOMAIN=housebook.example.test
HOUSEBOOK_PROXY_SECRET=
