# Obtain the first two values from the Google Cloud OAuth web client. Generate
# the cookie secret locally as described in README.md. Never commit live values.
OAUTH2_PROXY_CLIENT_ID=
OAUTH2_PROXY_CLIENT_SECRET=
OAUTH2_PROXY_COOKIE_SECRET=
